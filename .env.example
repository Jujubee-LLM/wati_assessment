# Optional. If unset, the CLI uses the deterministic fallback planner.
DASHSCOPE_API_KEY=
QWEN_MODEL=qwen-plus

# Optional. Reserved for a future real WATI HTTP adapter.
# Prefer the full API Endpoint URL from WATI API Docs if available.
WATI_API_BASE_URL=
WATI_TENANT_ID=
WATI_TOKEN=
